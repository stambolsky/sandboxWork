package ru.ql.tt.tests;import org.openqa.selenium.By;import org.openqa.selenium.remote.BrowserType;import org.testng.annotations.AfterClass;import org.testng.annotations.BeforeClass;import ru.ql.tt.appmanager.ApplicationManager;public class TestBase {    protected static final ApplicationManager app = new ApplicationManager(BrowserType.CHROME);    @BeforeClass    public static void setUp() throws Exception {        app.checkBrowser();        app.init();        app.goToProfile();    }    @AfterClass    public static void tearDown(){        app.stop();    }    /*protected void clickWaitElement(String s, int i) throws InterruptedException {        app.wd.findElement(By.xpath(s)).click();        Thread.sleep(i);    }*/    /*protected void refreshPage() throws InterruptedException {        app.wd.navigate().refresh();        Thread.sleep(5000);    }*/}